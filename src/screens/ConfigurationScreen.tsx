import React, {FC} from 'react';
import {View, Text} from 'react-native';

const ConfigurationScreen: FC = (): JSX.Element => {
  return (
    <View>
      <Text>Configuration IoT</Text>
    </View>
  );
};

export default ConfigurationScreen;
